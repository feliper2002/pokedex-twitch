import 'package:flutter/material.dart';

Widget modalBar() => Container(
      margin: const EdgeInsets.only(bottom: 6),
      height: 6,
      width: 80,
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(3),
      ),
    );
